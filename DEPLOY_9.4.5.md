# Pay-Pilot backend 9.4.5

Deploy the extracted files to the repository root (do not upload this ZIP as a single file).
After Railway deploys, /api/health should report version 9.4.5.
This release fixes Hosted Link completion/result parsing.
